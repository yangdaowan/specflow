# <Feature Name> — SPEC

## 背景与目标（Goal）
- 一句话目标：
- 业务背景：

## 范围（In Scope）
- [ ] 

## 非目标（Non-Goals）
- [ ] 

## 用户故事 / 使用场景
- 场景 1：
- 场景 2：

## 术语表（可选，但推荐）
| 术语 | 含义 |
| --- | --- |
|  |  |

## 领域模型 / 数据（如适用）
- 实体：
- 字段：
- 约束：

## 接口与交互（如适用）
- 交互形态：API / CLI / UI
- 输入：
- 输出：
- 鉴权与权限（如适用）：

## 状态机与异步任务（如适用）
- 状态字段：`status` / `retry_count` / `last_error`（示例）
- 状态流转：
- 幂等性：
- 重试策略：
- 失败处理：

## 失败模式与边界情况（必须）
- 输入无效：
- 依赖不可用：
- 并发/竞态：
- 权限不足：
- 超时/重试耗尽：

## 安全与隐私（如适用）
- 数据分类：
- 日志脱敏：
- 权限边界：

## 可观测性（如适用）
- 结构化日志（字段建议）：`trace_id`, `user_id`, `feature`, `status`, `error_code`
- 指标：成功率、失败率、延迟（P95/P99）、队列积压（如有）
- 告警（如适用）：

## 测试策略（必须）
- 单元测试：核心规则与边界
- 集成测试：关键链路（含失败路径）
- 回归测试：至少覆盖一个“此前易错点/关键验收项”

